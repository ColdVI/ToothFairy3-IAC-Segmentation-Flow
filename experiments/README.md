# Flow experiment source snapshots

Source snapshots from the project's Drive, reviewed on 2026-09-22.

- `iacb_sdf_bridge`: paired prior-to-GT stochastic SDF endpoint bridge, plus the case-balanced trainer.
- `canalmanifold_geoflow`: explicit tube/surface geometry and GeoFlow-Newton.

The existing `flow/` and `iacflow/` tracks remain separate. See `docs/FLOW_ARCHITECTURES_TR.md` and `docs/FLOW_RUN_LAYOUT.md` for architecture and retention details.

Training weights, CBCTs and caches stay outside GitHub. Source availability does not back up trained checkpoints.
